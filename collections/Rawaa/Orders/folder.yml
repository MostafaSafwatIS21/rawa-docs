info:
  name: Orders
  type: folder
  seq: 8

request:
  auth: inherit

docs:
  content: |-
    Orders module — Customer, Vendor, and Admin endpoints.

    **Auth tokens:**
    - Customer routes → `{{customerToken}}`
    - Vendor routes → `{{vendorToken}}`
    - Admin routes → `{{adminToken}}`

    After **Create order** runs, `orderId` is auto-saved and reused in all subsequent requests.
  type: text/markdown
