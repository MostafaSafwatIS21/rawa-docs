info:
  name: "[Vendor] List Own Settlements"
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/vendor/settlements"
  headers:
    - name: Authorization
      value: Bearer {{vendor_token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
