info:
  name: Get all brands
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/brands"
  params:
    - name: search
      value: <string>
      type: query
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
