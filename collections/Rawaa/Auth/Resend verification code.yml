info:
  name: Resend verification code
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/auth/resend"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "identifier": "mostafa.is1242003@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
