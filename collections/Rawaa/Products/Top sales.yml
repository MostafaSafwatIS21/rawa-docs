info:
  name: Top sales
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/products/top-sales?limit=10"
  params:
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
