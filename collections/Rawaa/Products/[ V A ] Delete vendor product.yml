info:
  name: "[ V A ] Delete vendor product"
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/products/:id"
  params:
    - name: id
      value: <string>
      type: path
      description: "(Required) "
  auth:
    type: bearer
    token: "{{bearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
