info:
  name: "[Admin] Deactivate customer profile"
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base_url}}/customers/:id"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
