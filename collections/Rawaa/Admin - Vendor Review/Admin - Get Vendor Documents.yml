info:
  name: Admin - Get Vendor Documents
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/admin/vendors/:id/documents"
  params:
    - name: id
      value: 4da969ca-0854-4861-9c6b-f2a7646ee9cf
      type: path
  auth:
    type: bearer
    token: "{{adminToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
